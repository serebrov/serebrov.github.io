{{#extend "bootstrap"}}
  {{#content "body"}}

    {{#each paginate.files}}
    <div class="row">
      <small class="pull-left">{{formatDate this.date "%Y-%m-%d"}}</small>
      <div>{{ this.intro }}</div>
      <a href="{{relative ../path this.path}}">Read more »</a>
    </div>
    {{/each}}

    <!--ul class="pagination">
      {{#if paginate.previous}}
        <li><a href="{{relative ../path paginate.previous.path}}">Prev</a></li>
      {{else}}
        <li class="disabled"><a href="#">Prev</a></li>
      {{/if}}
      {{#each paginate.pages}}
        <li><a href="{{relative ../path this.path}}">{{ this.paginate.num }}</a></li>
      {{/each}}
      {{#if paginate.next}}
        <li><a href="{{relative ../path paginate.next.path}}">Next</a></li>
      {{else}}
        <li class="disabled"><a href="#">Next</a></li>
      {{/if}}
    </ul-->
  {{/content}}
{{/extend}}

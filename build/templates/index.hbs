{{#extend "bootstrap"}}
  {{#content "body"}}

    {{#each paginate.files}}
    <div class="row index-row">
      <div class="col-md-12">
        <small class="pull-right">{{formatDate this.date "%Y-%m-%d"}}</small>
        <a class="title" href="{{relative ../path this.path}}">{{ this.title }}</a>
        <div>{{ this.intro }}</div>
        <a class="more" href="{{relative ../path this.path}}">Read more »</a>
      </div>
    </div>
    {{/each}}
    <div class="row">
        <div class="col-md-12">
            {{#embed "so"}}
            {{/embed}}
        </div>
    </div>

    <!--ul class="pagination">
      {{#if paginate.previous}}
        <li><a href="{{relative ../path paginate.previous.path}}">Prev</a></li>
      {{else}}
        <li class="disabled"><a href="#">Prev</a></li>
      {{/if}}
      {{#each paginate.pages}}
        <li><a href="{{relative ../path this.path}}">{{ this.paginate.num }}</a></li>
      {{/each}}
      {{#if paginate.next}}
        <li><a href="{{relative ../path paginate.next.path}}">Next</a></li>
      {{else}}
        <li class="disabled"><a href="#">Next</a></li>
      {{/if}}
    </ul-->
  {{/content}}
{{/extend}}
